"""Keyword-driven Playwright framework (Python).

Tests are written as rows in CSV files (test_cases/*.csv). Each row is one
keyword (click, enter, validate, ...) executed by framework.executor.
"""
__version__ = "1.0.0"
