# Keyword reference

> Generated by `python -m framework.cli keywords --markdown > specs/keywords.md`. Do not edit by hand.

## CSV format

```
test_id,test_name,tags,keyword,target,value,description
```

- One row = one step. Steps of a test are consecutive rows sharing a `test_id` (blank `test_id` continues the previous test).
- `test_name` and `tags` (separated by `;`) are read from the first row of a test.
- Cells containing a comma must be wrapped in double quotes; double any quote inside.
- A row whose first cell starts with `#` is a comment.
- **target** = which element (see locator forms below). **value** = text/URL/number/expected result.
- Variables: `${NAME}`, `${env.NAME}`, `${env.NAME:-default}`, `${config.base_url}`, `${timestamp}`, `${uuid}`, `${random}`, `${date}`.
- Tags: `smoke`, `regression`, `external` ... become pytest markers. `skip` / `fixme` disable the test.

## Locator forms (target column)

| Form | Example |
|---|---|
| Object repository | `@google.search_box` |
| Role | `role=button[name="Search"]` (quotes optional; `,exact=true` allowed) |
| Label / placeholder | `label=Username`, `placeholder=Search` |
| Text / alt / title | `text=Sign in`, `alt=Logo`, `title=Help` |
| Test id | `testid=submit` |
| CSS / XPath | `css=#search a h3`, `xpath=//h1`, or plain `#search` |
| Pick one match | append ` >> first`, ` >> last` or ` >> nth=2` |

Preference order: role > label > placeholder > text > testid > css/xpath. Put every locator in `object_repository/*.csv` and reference it as `@name`.

## Browser

| Keyword | Aliases | target | value | What it does | Example (keyword,target,value) |
|---|---|---|---|---|---|
| `close_browser` | - | none | none | Close the browser. The framework also closes it automatically when a test ends or fails. | `close_browser,,` |
| `go_back` | - | none | none | Browser Back button. | `go_back,,` |
| `go_forward` | - | none | none | Browser Forward button. | `go_forward,,` |
| `navigate` | `open_url`, `goto`, `launch_url` | none | required | Go to a URL (https:// optional; '/path' is joined with KDF_BASE_URL). | `navigate,,https://www.google.com` |
| `open_browser` | - | none | optional | Launch the browser. Optional value: chrome, msedge, chromium, firefox or webkit (default comes from KDF_BROWSER / KDF_CHANNEL). Tests that skip this step get a browser automatically. | `open_browser,,chrome` |
| `refresh` | `reload` | none | none | Reload the current page. | `refresh,,` |

## Interaction

| Keyword | Aliases | target | value | What it does | Example (keyword,target,value) |
|---|---|---|---|---|---|
| `check` | - | required | none | Tick a checkbox / radio button. | `check,label=Remember me,` |
| `clear` | - | required | none | Empty an input field. | `clear,label=Search,` |
| `click` | `tap` | required | none | Click an element. | `click,role=button[name=Login],` |
| `click_if_visible` | - | required | optional | Click only if the element shows up (default wait 3 s, or the number of seconds in value). Never fails - use it for cookie banners and consent pop-ups. | `click_if_visible,@google.consent_accept_all,3` |
| `double_click` | - | required | none | Double-click an element. | `double_click,text=Row 1,` |
| `enter` | `fill`, `input_text`, `type_text` | required | optional | Put text into an input (replaces existing text; empty value clears the field). | `enter,label=Username,alice` |
| `enter_secret` | - | required | required | Like enter, but the value is masked in logs and reports. Prefer ${env.PASSWORD}. | `enter_secret,label=Password,${env.APP_PASSWORD}` |
| `hover` | - | required | none | Move the mouse over an element. | `hover,text=Menu,` |
| `press_key` | - | optional | required | Press a key or chord (Enter, Tab, Escape, Control+A ...). With a target the element is focused first; without one the key goes to the page. | `press_key,@google.search_box,Enter` |
| `right_click` | - | required | none | Right-click an element. | `right_click,text=Row 1,` |
| `scroll_to` | - | required | none | Scroll an element into view. | `scroll_to,text=Footer,` |
| `select` | `select_option` | required | required | Choose an option of a <select> by its label or value. | `select,label=Country,India` |
| `type_slowly` | - | required | required | Type key by key (50 ms apart) - for widgets that react to individual key presses. | `type_slowly,@search.box,playwright` |
| `uncheck` | - | required | none | Untick a checkbox. | `uncheck,label=Remember me,` |
| `upload_file` | - | required | required | Attach a file to an <input type=file>. | `upload_file,label=Resume,data/cv.pdf` |
| `wait` | - | none | required | Hard pause for N seconds. Last resort - flaky and slow; use wait_for or validate instead. | `wait,,2` |
| `wait_for` | `wait_for_visible` | required | none | Wait until an element is visible (prefer this over wait). | `wait_for,@google.results_container,` |
| `wait_for_url` | - | none | required | Wait until the URL matches a glob (e.g. **/board). | `wait_for_url,,**/board` |

## Validation

| Keyword | Aliases | target | value | What it does | Example (keyword,target,value) |
|---|---|---|---|---|---|
| `validate` | `verify` | required | optional | The element is visible. If value is given, its text must also contain it (case-sensitive). | `validate,@google.results_container,` |
| `validate_attribute` | `verify_attribute` | required | required | An attribute has a value. Write value as name=expected. | `validate_attribute,role=link[name=Docs],href=/docs` |
| `validate_checked` | `verify_checked` | required | none | A checkbox / radio is ticked. | `validate_checked,label=Remember me,` |
| `validate_contains` | `verify_contains` | required | required | The element's text contains value (case-sensitive). | `validate_contains,@results,Playwright` |
| `validate_contains_ignore_case` | `verify_contains_ignore_case` | required | required | The element's text contains value, ignoring upper/lower case. | `validate_contains_ignore_case,@google.results_container,playwright` |
| `validate_count` | `verify_count` | required | required | Exactly N elements match. | `validate_count,css=table tbody tr,5` |
| `validate_count_at_least` | `verify_count_at_least` | required | required | At least N elements match (waits until that many exist). | `validate_count_at_least,@google.result_titles,3` |
| `validate_disabled` | `verify_disabled` | required | none | The element is disabled. | `validate_disabled,role=button[name=Save],` |
| `validate_enabled` | `verify_enabled` | required | none | The element is enabled. | `validate_enabled,role=button[name=Save],` |
| `validate_hidden` | `verify_hidden` | required | none | The element is not visible (or not in the page at all). | `validate_hidden,@login.error,` |
| `validate_page_contains` | `verify_page_contains` | none | required | The text is visible somewhere on the page (case-insensitive). | `validate_page_contains,,Welcome back` |
| `validate_text` | `verify_text` | required | required | The element's text equals value (whitespace-normalised). | `validate_text,role=heading,Welcome` |
| `validate_title` | `verify_title` | none | required | The page title equals value. | `validate_title,,Dashboard` |
| `validate_title_contains` | `verify_title_contains` | none | required | The page title contains value (case-insensitive). | `validate_title_contains,,Google Search` |
| `validate_url` | `verify_url` | none | required | The full URL equals value. | `validate_url,,https://example.com/board` |
| `validate_url_contains` | `verify_url_contains` | none | required | The URL contains value (case-insensitive). | `validate_url_contains,,/search` |
| `validate_value` | `verify_value` | required | required | An input's current value equals value. | `validate_value,label=Search,playwright` |
| `validate_visible` | `verify_visible` | required | none | The element is visible. | `validate_visible,@google.search_box,` |

## Data & Utility

| Keyword | Aliases | target | value | What it does | Example (keyword,target,value) |
|---|---|---|---|---|---|
| `log` | - | none | required | Write a message to the test log. | `log,,Checkpoint reached` |
| `screenshot` | - | none | optional | Save a screenshot to reports/screenshots/<test_id>/ (value = file name, optional). | `screenshot,,google-results.png` |
| `set_variable` | - | required | required | Store a value. The 'target' column holds the variable NAME. Read it later as ${NAME}. Use ${env.NAME:-default} to allow overriding from the environment. | `set_variable,SEARCH_TEXT,${env.SEARCH_TEXT:-Playwright Python}` |
| `store_text` | - | required | required | Save an element's text into the variable named in value. | `store_text,@order.number,ORDER_ID` |
