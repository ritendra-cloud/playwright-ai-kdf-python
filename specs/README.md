# specs/

| Path | Written by | Purpose |
|---|---|---|
| `testing/*.md` | planner agent | Test plans: scenarios, steps, expected results |
| `keywords.md` | `python -m framework.cli keywords --markdown` | Keyword + CSV reference (the agents' vocabulary) |

Plans are the contract between the planner and the generator. Keep them in review-friendly markdown.
